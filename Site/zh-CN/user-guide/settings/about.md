# 关于

显示 App、Core、Service、CLI 四个组件的版本号(颜色会反映服务当前是否健康)、SwiftList 的简短说明，以及项目主页和在线使用手册的链接。

## 配置文件夹

紧接着下方还有两个链接，用来打开 SwiftList 读写自身配置的文件夹——链接文字本身就是实际路径，点击时如果文件夹还不存在会先创建出来:

- **用户配置文件夹** —— 存放 `user-settings.json` 的每用户文件夹。每次保存设置时，都会先把当前文件轮转备份为 `user-settings.json.bak.1`(旧的备份依次往后顺延，最多保留到 `.bak.5`)，再写入新文件，所以一次误改或者保存过程中崩溃，总能找到一份最近的备份恢复。
- **系统配置文件夹** —— 后台服务使用的共享、机器级文件夹(`machine-settings.json`、索引缓存，以及服务端日志)。

## 检查更新

- **检查更新** 按钮 —— 查询是否有新版本;按钮文字本身会反映进度("正在检查更新..."，之后显示
  "已是最新版本"或找到的新版本号)。
- 如果非管理员账户无法停止后台服务来原地安装更新，会显示一条警告横幅，并引导你前往手动下载页面。
- 一旦发现新版本:
  - **静默自动更新** —— 后台下载安装，显示进度条，完成后自动重启 SwiftList。
  - **前往下载页面** —— 在浏览器中打开 GitHub release 页面，手动安装。

这个页面和[通用 → 系统](./general#系统)里的**启动时自动检查更新** / **自动静默更新**两个复选框是配套的——那两项控制是否在启动时自动执行这次检查;这个页面则是让你随时手动触发一次。

# URI 協議（swiftlist://）

SwiftList 會自動把自己註冊為 `swiftlist://` 鏈接的處理程式——不需要額外的安裝步驟，第一次運行時就會自動完成註冊。這樣任何能打開鏈接的東西（瀏覽器、捷徑、別的程式、腳本）都能直接跳到 SwiftList
的某個具體位置，而不是只能靠熱鍵觸發。

如果 SwiftList 還沒運行，打開一個 `swiftlist://` 鏈接會先啓動它，再執行這個鏈接指向的操作。如果已經在運行，正在運行的那個實例會直接處理這個鏈接——不會再啓動第二份處理程序。

## 支援的鏈接

| 鏈接 | 作用 |
|---|---|
| `swiftlist://` | 激活快速搜尋視窗——效果和用熱鍵呼出它一樣。 |
| `swiftlist://search/[關鍵詞]` | 激活快速搜尋視窗，並預填 `[關鍵詞]`。 |
| `swiftlist://fullsearch/[關鍵詞]` | 打開完整搜尋視窗，並預填 `[關鍵詞]`。 |
| `swiftlist://settings/page/[分區]` | 打開設定視窗，並切到指定的頂層分區。 |
| `swiftlist://settings/entry/[序號]` | 打開設定視窗，並直接跳轉到某一項具體設定，並高亮顯示。 |

```
swiftlist://search/report
swiftlist://settings/page/Appearance
```

第一個會激活快速搜尋視窗，並已經用"report"過濾好；第二個會直接打開設定視窗的"外觀"頁。

`[分區]` 對應側邊欄頂層的某一項：`Service`、`Index`、`General`、`Appearance`、`Hotkeys`、
`Plugins`、`Favorites`、`History`、`QuickPanel`、`About`——不區分大小寫。

`[序號]` 不是給人手動輸入用的——它是[設定搜尋](./instant-answers)在你選中某項設定結果時自己生成的一個數字，選中結果會自動帶上這個序號，原樣跳轉回那一項設定。這個序號在重啓之間並不穩定，不要指望某個具體數字每次都對應同一項設定。

## 無法識別的鏈接

任何匹配不上已知路由的鏈接——打錯了字、分區不存在、`swiftlist://` 後面跟了一堆亂七八糟的內容——都會被直接忽略。由於任何網站或程式都能在你不知情的情況下調起這個協議，一個錯誤或異常的鏈接不應該產生任何出人意料的效果；這類情況只會記進日誌方便你自己排查，除此之外什麼都不會發生。

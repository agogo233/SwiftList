<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- The command name directly, since the installer now just adds {app} (where this sits alongside
         SwiftList.App.exe etc.) to PATH: no more copy-then-rename-the-apphost step at runtime. -->
    <AssemblyName>slf</AssemblyName>
    <RootNamespace>SwiftList.Cli</RootNamespace>
    <Version>1.0.2</Version>
    <GenerateDependencyFile>false</GenerateDependencyFile>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Core\Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="SwiftList.Cli.Tests" />
  </ItemGroup>

</Project>
